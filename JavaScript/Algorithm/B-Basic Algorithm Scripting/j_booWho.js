function booWho(bool) {
  let b = bool === true || bool === false; // b = booleano
  return b;
}

console.log(booWho(3));

// comprueba si el valor está clasificado como booleano primitivo. Devuelve true o false.